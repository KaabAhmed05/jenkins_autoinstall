-This is the code to download jenkins on an aws instance
  
    manually it takes a lot of time and some time a bit difficult
     inside the .pem folder just add your own .pem key so that the connection which we have stablished for remote exec will work beckause it will take the .pem to access it
